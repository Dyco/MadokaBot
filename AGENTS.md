# 项目约定

- 文件读写显式使用 UTF-8；保持代码精简可读，新增或调整的方法、枚举使用中文注释。
- 非绝对必要不使用注入式写法。修改前先理解框架与业务边界；方案不符合现有框架时，先与用户确认。
- `madokabot/core` 存放公共支持，`madokabot/default` 存放默认功能，`madokabot/plugins` 存放扩展插件。基础层不得引用业务插件，扩展插件不得依赖默认功能。
- 复读插件固定命名为 `copying`，主命令保持 `copying`，不得改为 `repeater`。
- 除 `on_message(rule=fullmatch(...))` 完整匹配指令外，命令必须遵循全局 `COMMAND_START`。所有 `on_alconna` 注册显式设置 `use_cmd_start=True`，不得关闭起始符或在别名中硬编码起始符。
- 自动复读、链接自动解析和通知等事件监听不属于指令；会话内的回复翻页也不作为独立命令注册。
- 目录与内部符号改名不得随意改变已有数据库表名、数据目录、配置项或持久化键。
- Steam 数据按新结构保存，不兼容、迁移或读取旧数据：群相关数据使用公共群存储的 `steam` 节点，跨群玩家状态使用公共资源目录与 `JsonDataStore` 独立保存。
